{
  "name": "now-todos",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "deploy-server": "now -e APOLLO_ENGINE_KEY=@apollo-engine-key -e NODE_ENV=production -n now-todos-server server",
    "deploy-client": "yarn workspace client build && now -e NODE_ENV=production client/build && echo \"Cleaning up build dir\" && rm -rfv client/build",
    "start": "concurrently \"cd server && yarn start\" \"cd client && yarn start\"",
    "clean": "rm -rf {./,server,client}/node_modules"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
