{
  "name": "now-todos",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "deploy-server": "./server/tasks/deploy.sh",
    "deploy-client": "REACT_APP_GRAPHQL_API_URL=https://now-todos-server.now.sh/graphql yarn workspace client build && now -e NODE_ENV=production -n now-todos-client client/build && echo \"Cleaning up build dir\" && rm -rfv client/build",
    "start": "concurrently \"cd server && yarn start\" \"cd client && yarn start\"",
    "clean": "rm -rf {./,server,client}/node_modules"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}