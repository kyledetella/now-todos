{
  "name": "now-todos",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "ts-client"
  ],
  "scripts": {
    "deploy-server": "now -e APOLLO_ENGINE_KEY=@apollo-engine-key -e NODE_ENV=production server",
    "deploy-ts-client": "yarn workspace ts-client build && now -e NODE_ENV=production ts-client/build && echo \"Cleaning up build dir\" && rm -rfv ts-client/build",
    "start": "concurrently \"cd server && yarn start\" \"cd client && yarn start\"",
    "clean": "rm -rf {./,server,client,ts-client}/node_modules"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
